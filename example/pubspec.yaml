name: firebase_dynamic_links_fixed_example
description: Demonstrates how to use the firebase_dynamic_links_fixed plugin.

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_dynamic_links_fixed:
    path: ../
  firebase_core: ^4.4.0
  url_launcher: ^6.2.0

flutter:
  uses-material-design: true
